# Copy this file to .env.local and fill in your key.
# The key stays on the server — it is read by the API route via
# process.env.ANTHROPIC_API_KEY and is never exposed to the browser.
ANTHROPIC_API_KEY=sk-ant-...
